<header class="site-header">
    <div class="nav">
        <a href="{{ route('home') }}" class="brand">MyPortfolio</a>
        <nav>
            <a href="{{ route('home') }}">Home</a>
            <a href="{{ route('about') }}">About</a>
            <a href="{{ route('projects') }}">Projects</a>
            <a href="{{ route('contact') }}">Contact</a>
        </nav>
    </div>
</header>
